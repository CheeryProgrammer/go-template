# HTTP
HTTP_ADDR=:8080
HTTP_READ_TIMEOUT=5s
HTTP_WRITE_TIMEOUT=10s
SHUTDOWN_TIMEOUT=30s

# Database (optional — leave empty to start without postgres)
DATABASE_URL=postgres://app:app@localhost:5432/app?sslmode=disable
